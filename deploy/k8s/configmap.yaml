apiVersion: v1
kind: ConfigMap
metadata:
  name: secureshare-config
  namespace: secureshare
data:
  PORT: "8000"
  STORAGE_DIR: "/data"
  LINK_TTL_DAYS: "7"
  # LOGO_URL: "https://example.com/logo.png"
  # TITLE: "Acme Corp"
